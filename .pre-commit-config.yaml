repos:
  # Detect secrets before they get committed
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks

  # JS/TS lint + format (uncomment after adding biome.json)
  # - repo: local
  #   hooks:
  #     - id: biome
  #       name: biome check
  #       entry: npx biome check --write
  #       language: system
  #       types_or: [javascript, jsx, typescript, tsx, json]

  # Python lint + format (uncomment after adding pyproject.toml with ruff config)
  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.8.6
  #   hooks:
  #     - id: ruff
  #       args: [--fix]
  #     - id: ruff-format
